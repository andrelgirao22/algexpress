// Componente de card reutilizável
// Usado para exibir informações em containers
export interface CardProps {
  // Props do card serão definidas aqui
}

export const Card = () => {
  // Implementação do componente Card
  return null;
};

export const CardHeader = () => {
  return null;
};

export const CardContent = () => {
  return null;
};

export const CardFooter = () => {
  return null;
};
